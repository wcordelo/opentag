node_modules
.env
.env.local

# E2E harness artifacts
e2e/.chrome-profile
e2e/results
state.db
state.db-shm
state.db-wal
data/blobs
.tools

# Railway local link metadata (never commit)
.railway/
**/.railway/

# Local knowledge-source grant authority (private key)
.opentag-knowledge-source-authority.json
