{
  "name": "opentag-edge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "OpenTag edge Workers: research orchestrator (Durable Objects) + @opentag/bot-store-durable-object (bot StateStore).",
  "license": "MIT",
  "main": "./src/store/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "dev:bot-store": "wrangler dev --config wrangler.bot-store.toml",
    "deploy": "npm run build:wasm && wrangler deploy",
    "deploy:bot-store": "wrangler deploy --config wrangler.bot-store.toml",
    "build:wasm": "cd wasm-core && tinygo build -o ../workers/wasm-dispatch/src/main.wasm -target wasm -no-debug . && wasm-opt -O3 ../workers/wasm-dispatch/src/main.wasm -o ../workers/wasm-dispatch/src/main.opt.wasm",
    "typecheck": "tsc --noEmit",
    "check-types": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:workers": "vitest run --config vitest.workers.config.ts",
    "test:e2e": "vitest run --config vitest.workers.bot-store.config.ts",
    "test:all": "vitest run && vitest run --config vitest.workers.config.ts && vitest run --config vitest.workers.bot-store.config.ts",
    "test:e2e:full": "bash scripts/e2e-full.sh",
    "migrate:dry": "echo 'Migration is an external action — pass explicit adapters via scripts/migrate-pg-to-do.ts after sign-off'"
  },
  "dependencies": {
    "@cloudflare/sandbox": "^0.1.0",
    "hono": "^4.6.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.4",
    "@cloudflare/workers-types": "^5.20260708.1",
    "@types/node": "^22.20.1",
    "binaryen": "^122.0.0",
    "typescript": "^5.6.3",
    "vitest": "^4.1.3",
    "wrangler": "^4.0.0"
  }
}
