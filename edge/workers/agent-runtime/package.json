{
  "name": "opentag-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Always-on AG-UI triage runtime on Cloudflare Containers (AGENT_URL target).",
  "scripts": {
    "deploy": "wrangler deploy --config wrangler.toml",
    "dev": "wrangler dev --config wrangler.toml",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260708.1",
    "typescript": "^5.6.3",
    "wrangler": "^4.0.0"
  }
}
