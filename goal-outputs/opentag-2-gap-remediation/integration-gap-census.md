# OpenTag 2 gap-remediation integration census

Snapshot: 2026-07-14, current shared worktree after the control, harness/attachment, and delivery bundles. This is a source/call-site census, not the independent Tier 2 review. `RESOLVED` means the audited production path and regression coverage implement the finding or the locked no-deploy disposition. `PARTIAL` means useful primitives exist but at least one production path, failure mode, test, or truthful-doc requirement remains. No finding is silently omitted.

## Finding-by-finding census

| Finding | Status | Decisive current evidence | Exact remaining integration |
| --- | --- | --- | --- |
| C1 | **RESOLVED** | Repository coding and selected Claude turns choose the harness and visibly reject missing capability/repository instead of reaching AG-UI (`edge/src/agent-turn.ts:641-658`); harness failure is authoritative (`edge/src/agent-turn.ts:1028-1034`). The production binding remains intentionally commented pending explicit deploy approval (`edge/wrangler.bot.toml:54-62`). | None at source level. Activation remains an explicit operator/deploy gate; do not uncomment or deploy without approval. Correct the contradictory operations wording under L2. |
| C2 | **PARTIAL** | Durable identity/reconciliation primitives now exist, and ingress reserves a UUID-format live ID before its first await (`edge/src/slack/pre-admit-turn.ts:134-157`; `edge/src/slack/cloudflare-slack-adapter.ts:339-386`). However, the production Channels renderer still creates its placeholder with raw `this.client.postMessage`, bypassing `postLiveMessage` and durable `ts` confirmation (`edge/src/slack/cloudflare-slack-adapter.ts:1128-1170`); direct `post()` does the same (`edge/src/slack/cloudflare-slack-adapter.ts:876-908`). | Route every first visible live create (Channels text/error/tool-only and direct/harness final post) through the reserved-ID helper, reconcile duplicate/ambiguous results before recovery, and confirm the exact `ts`. Add a lifecycle+adapter regression proving an applied-but-response-lost live create is updated by recovery and never duplicated. |
| C3 | **PARTIAL** | When configured, `fenced()` persists `done` while holding the render token and before Slack (`edge/src/slack/cloudflare-slack-adapter.ts:389-414`), with failure-closed coverage (`edge/test/slack-stream.test.ts:52-85`). But `sessionEvents` is optional and `persistSessionTerminal()` silently returns when absent (`edge/src/slack/cloudflare-slack-adapter.ts:130-145,267-292`); composition also conditionally supplies it (`edge/src/bot-engine.ts:108-123`). | Make SessionEventDO mandatory in production adapter construction and remove the missing-binding success path. Readiness must fail before turns run. Add a negative composition test proving no final Slack request occurs when the binding is absent/unusable. |
| C4 | **RESOLVED** | Recovery losslessly pages output (`edge/src/store/conversation-state-do.ts:1321-1346,1382-1435`), keeps stable per-page IDs, preserves ownership on ambiguous/429 outcomes, and retains a slow dead letter after definitive retry exhaustion (`edge/src/store/conversation-state-do.ts:1192-1232`). Page reconstruction and retained-dead-letter tests exist in `edge/test/stream-render-pages.test.ts` and `edge/test/render-obligation.test.ts`. | None for the original recovery-size/silent-drop finding. Shared scheduling remains H4. |
| H1 | **PARTIAL** | Request-time Stop calls the exact AG-UI control endpoint and requires matching `accepted:true`, `quiescent:true`, and execution ID (`edge/src/slack/stop-routing.ts:36-64,327-375`); runtime tracks the exact request/controller until quiescence (`runtime.ts:81-124`). Alarm-resumed Stop interrupts a harness when `sessionId` exists, but has no AG-UI `else` branch and can mark control complete without calling `/opentag/control/interrupt` (`edge/src/store/conversation-state-do.ts:1035-1063`). Missing `SESSION_EVENTS` is also silently skipped in request-time routing (`edge/src/slack/stop-routing.ts:327-358`). | Reuse the exact AG-UI interrupt contract in alarm continuation whenever there is no harness session, and make missing SessionEventDO fail closed. Add alarm-resume tests for accepted, mismatched, non-quiescent, and unavailable AG-UI control; update the obsolete Stop integration fixture described below. |
| H2 | **RESOLVED** | Bare `--model` selects Claude Code; `--codex` and every `-rsn` are visible errors (`edge/src/slack/overrides.ts:84-129`). Validation occurs before sticky persistence (`edge/src/agent-turn.ts:565-594`), and confirmation says `Active` only after validation (`edge/src/agent-turn.ts:443-456`). Focused parser/stickiness/no-persistence tests exist in `edge/test/overrides.test.ts` and `edge/test/agent-turn-overrides.test.ts`. | None. |
| H3 | **PARTIAL** | Bounded inline/staged attachment types, streamed caps, harness envelopes, exact byte materialization, and late-file matching primitives exist (`edge/src/slack/download-files.ts:33-55,101-149,198-256`; `edge/workers/sandbox/harness-server.ts:145-169,1460-1475`; `edge/src/slack/late-file-repair.ts:24-52`). Production still calls `buildFileContentParts` without a durable stager (`edge/src/slack/cloudflare-slack-adapter.ts:503-510`); no production file imports/calls the late-repair functions; staged attachments deliberately fail unresolved (`edge/workers/sandbox/harness-server.ts:145-169`). | Back staging with `BLOBS` (or another durable namespace), resolve staged bytes at the authenticated harness boundary, persist fileless-mention correlation, hydrate delayed files, wait for thread idle, and pre-admit one exact synthetic turn. Add end-to-end tests from Slack event through staged/late repair to harness-visible bytes. |
| H4 | **PARTIAL** | HTTP 429/`Retry-After` replay and `SlackChannelRateScheduler` exist (`edge/src/slack/web-api.ts:108-145,243-274`). No production call constructs a scheduler or passes `options.scheduler`; adapter, lifecycle, Stop, quick actions, and agent-turn each create independent clients (`edge/src/slack/cloudflare-slack-adapter.ts:190-192`; `edge/src/slack/turn-lifecycle.ts:92`; `edge/src/slack/stop-routing.ts:226-228`; `edge/src/agent-turn.ts:677`). ConversationStateDO recovery also uses raw fetch (`edge/src/store/conversation-state-do.ts:1400-1435`). | Create one composition-root per-channel scheduler shared by every request-path Slack client, and an equivalent durable discipline for DO alarm egress. Test concurrent writes from independent live/status/Stop/recovery paths to the same channel plus 429 spacing. |
| H5 | **RESOLVED** | Execute, terminal append, and interrupt multi-row transitions each run in the required transaction (`edge/src/store/session-event-do.ts:267-284,325-344,383-409`), backed by `storage.transactionSync` (`edge/src/store/session-event-do.ts:545`). Fault-injection rollback tests cover execute and done (`edge/test/session-event-do.test.ts:161-211`). | None for the audited atomicity gap. |
| H6 | **RESOLVED** | The manifest adds `users.profile:read` (`slack-app-manifest.yaml:56-59`); requester resolution calls form-encoded `users.profile.get` with `include_labels=true` (`edge/src/slack/web-api.ts:338-378`); PR POST authorization requires exactly one equal standalone attribution line before egress (`edge/workers/sandbox/src/egress-policy.ts:230-249`). Regression coverage exists in `edge/test/slack-web-api.test.ts` and `edge/test/harness-egress-policy.test.ts`. | Source is complete. Reinstall/token refresh remains an explicit external activation step and was not performed. |
| H7 | **PARTIAL** | Lossless deterministic paging exists (`edge/src/slack/stream-render.ts:64-97`) and recovery uses it. The bespoke `stream()` has begun posting continuation pages (`edge/src/slack/cloudflare-slack-adapter.ts:973-999`), but the actual production AG-UI Channels renderer still forwards the dependency's one-message blocks unchanged (`edge/src/slack/cloudflare-slack-adapter.ts:1057-1100`) and raw Channels placeholder/final paths do not own stable continuation reconciliation (`edge/src/slack/cloudflare-slack-adapter.ts:1128-1215`). | Apply paging at the Channels final commit, reserve stable IDs for every continuation, persist per-page confirmation/resume state, and keep terminal SessionEventDO/active-turn ownership until all pages confirm. Add a production-renderer 200k-character reconstruction test; do not satisfy it only through adapter `stream()` or recovery. |
| H8 | **RESOLVED** | Quick-action parsing, stable identity, and transactional pre-admission happen before HTTP 200; persistence failure returns 503 and only the already-owned handoff enters `waitUntil` (`edge/src/worker.ts:313-363`; `edge/src/slack/quick-actions.ts:188-226`). Tests cover separated durable preparation/handoff in `edge/test/quick-actions.test.ts:256-307`. | None. |
| M1 | **RESOLVED** | The selected production equivalent is explicitly the Channels incremental renderer, which accumulates text, coalesces tool lifecycle, and enables tool status (`edge/src/slack/cloudflare-slack-adapter.ts:1183-1195`). Documentation truthfully distinguishes it from bespoke `stream()` and calls richer harness task/plan events future work (`docs/centaur-port.md:285-290`). Existing renderer tests exercise text, error, and tool-only terminal paths (`edge/test/slack-stream.test.ts:88-178`). | None under the audit's allowed “chosen equivalent” disposition; H4/H7 own scheduler/paging correctness. |
| M2 | **RESOLVED** | Initial status is best-effort and cannot abort execution (`edge/src/slack/turn-lifecycle.ts:445-460`); Channels tool activity is enabled (`edge/src/slack/cloudflare-slack-adapter.ts:1190-1195`); alarm-resumed Stop clears root-thread status before visible acknowledgement (`edge/src/store/conversation-state-do.ts:1100-1133`). | None for the original static/fatal-status finding. |
| M3 | **PARTIAL** | Agent reconstruction now reads SessionEventDO and excludes the active execution (`edge/src/agent-turn.ts:749-766`; `edge/src/slack/session-history.ts:86-113`). But admission serializes array prompts to text and drops non-text attachment metadata (`edge/src/slack/turn-lifecycle.ts:40-58,400-405`), while mirroring persists only output text and swallows failures (`edge/src/slack/cloudflare-slack-adapter.ts:233-259`). Tool results/attachment references are therefore not canonical. | Extend the event contract and lifecycle to persist structured input parts, tool calls/results, and attachment references, then reconstruct AG-UI/harness state from those events. Test isolate-loss reconstruction with a prior tool result and attachment, not only text. |
| M4 | **PARTIAL** | Rich bounded extraction and attachment normalization are implemented (`edge/src/slack/session-history.ts:13-75`). The production `getMessages()` loop still flattens raw Slack replies to `m.text` and discards blocks, legacy attachments, and files (`edge/src/slack/cloudflare-slack-adapter.ts:1252-1284`); `normalizeSlackHistoryMessage` has no production caller. | Normalize each history row, re-download/re-stage bounded prior attachments, merge them into follow-up content, and persist their durable references under M3. Add a fresh-isolate “compare prior PDF with this PDF” regression. |
| M5 | **PARTIAL** | AG-UI final updates can decorate configured artifact URLs with stable synthetic-turn cards and a per-thread/artifact marker (`edge/src/slack/cloudflare-slack-adapter.ts:1061-1110`); quick buttons re-enter exact ingress. The only production caller is this AG-UI update branch; direct/harness/recovery/research final posts do not decorate, and no Dig-deeper/Export research cards exist (`edge/src/slack/quick-card.ts:23-47,91-147`). | Move final-output card decoration into a shared fenced final-delivery layer used by AG-UI, harness, research, and recovery; define research Retry/Dig-deeper/Export actions and durable per-artifact identities. Add call-site tests, not builder-only tests. |
| M6 | **PARTIAL** | Outcome types, Analytics Engine write support, required `DELIVERY_METRICS`, Wrangler bindings, and bounded durability probes exist (`edge/src/store/conversation-state-do.ts:101-130`; `edge/src/env.ts:18-21`; `edge/src/health.ts:25-43`; `edge/wrangler.bot.toml:10-12`). Only recovery calls `emitDeliveryOutcome` (`edge/src/store/conversation-state-do.ts:1450-1457`); live `streamed`/`answer_visible`/`failed_size_limit` are not emitted. `/health` remains static and never calls `probeDurabilityHealth` (`edge/src/worker.ts:67-75`). | Emit outcomes only after confirmed live transitions, wire `/health` to the bounded probe with non-2xx on failure, and test actual route behavior plus Analytics points for live success/size failure. |
| M7 | **PARTIAL** | `Env.SESSION_EVENTS` and `forwardedMessageId` in SessionEventDO are required (`edge/src/env.ts:18-21`; `edge/src/store/session-event-do.ts:194-201`). Runtime/lifecycle still accept absence: session admission returns `accepted`, terminalization returns, replay skips, Stop skips, and adapter composition is conditional (`edge/src/slack/turn-lifecycle.ts:105-116,178-184,223-230`; `edge/src/slack/stop-routing.ts:327-358`; `edge/src/bot-engine.ts:108-123`). Static `/health` cannot expose the misconfiguration (`edge/src/worker.ts:67-75`). | Remove optional guards from invariant production paths, require exact forwarded identity in lifecycle signatures, and fail startup/readiness when required DO/metrics/agent bindings are absent. Add negative misconfiguration tests proving no model or Slack final boundary runs. |
| M8 | **RESOLVED** | Lifecycle persists immutable admission before the cross-DO call and waits only for exact accepted/duplicate/cancelled/exhausted state; it never retries model/tool execution (`edge/src/slack/turn-lifecycle.ts:105-175`). The owning engine has token CAS, stale-claim recovery, three bounded attempts, and retained exhausted evidence (`edge/src/store/session-handoff-engine.ts:3-27,68-97,108-187`), with alarm integration in `edge/src/store/conversation-state-do.ts:615-655`. | None for the audited pre-runtime handoff retry gap. |
| L1 | **PARTIAL** | Signed seven-day viewer tokens and a private no-store read endpoint exist (`edge/src/slack/session-link.ts`; `edge/src/worker.ts:47-64`). The link is injected only by Channels final update/error/tool-only paths (`edge/src/slack/cloudflare-slack-adapter.ts:1057-1100,1128-1170`); direct `post()`—used by harness and other final surfaces—has no session-context decoration (`edge/src/slack/cloudflare-slack-adapter.ts:876-908`). | Centralize once-per-thread session-link decoration across every first/final assistant surface, including harness/direct/recovery, while preserving the 50-block budget. Add a first-turn harness/direct-post regression and token route authorization tests. |
| L2 | **PARTIAL** | SessionEventDO now correctly names SQL as authoritative (`edge/src/store/session-event-do.ts:13-20`), and the port ledger narrows A1-A5 status and lists attachment/harness limits (`docs/centaur-port.md:1-11,279-290`). However, operations says disconnecting `HARNESS` “returns the bot to AG-UI-only operation,” contradicting the locked fail-visible behavior (`docs/operations.md:386-390` versus `edge/src/agent-turn.ts:641-658`); the ledger also calls delivery minimum counters implemented while live outcomes remain absent (`docs/centaur-port.md:73`; M6). | Correct the rollback text to say coding/selected-Claude turns fail visibly, and describe which metrics/outcomes are actually wired versus planned. Add a documentation consistency check for no-silent-fallback and required bindings. |
| L3 | **PARTIAL** | Cursor-safe retention logic preserves active/recent executions and bounds old tombstones (`edge/src/store/session-event-do.ts:415-467`), with engine coverage (`edge/test/session-event-do.test.ts:538-562`). No production caller invokes `compact`; only the RPC and test do (`edge/src/store/session-event-do.ts:614`; repository call-site census). | Schedule compaction from the owning DO using a proven safe replay cursor, persist/advance that cursor, and test alarm-driven compaction plus replay/dedup safety across the retention boundary. |

Status totals: **9 RESOLVED, 14 PARTIAL, 0 UNRESOLVED**. “0 unresolved” does not mean completion: the 14 partial findings still reproduce their audited risk on at least one production path.

## Cross-cutting verification and test gaps

- `cd edge && npm run typecheck` currently fails at `edge/src/slack/turn-lifecycle.ts:240`: an explicitly possibly-undefined `liveClientMessageId` is passed under `exactOptionalPropertyTypes`. This blocks the required green typecheck.
- `cd edge && npm test` currently reports **589 passed, 2 failed**. `edge/test/slack-stream.test.ts:653` expects continuation posts but the production/fixture path records none; this is directly relevant to H7. `edge/test/bot-engine-remote-git.test.ts:546` still expects a Stop acknowledgement after its fake cannot satisfy the new exact AG-UI interrupt contract (`Invalid URL` in the run); update the fake to accept `Request` and return matching quiescence, then retain a separate fail-closed assertion for H1.
- `cd edge && npm run test:e2e` passes **24/24**. `cd edge/workers/sandbox && npm run typecheck` passes. The all-green success criterion is therefore not yet met.
- Missing integration coverage aligns with the remaining call-site gaps: no applied-but-lost Channels placeholder reconciliation (C2), no alarm-resumed AG-UI interrupt (H1), no live staging/late-file/history path (H3/M3/M4), no composition-wide scheduler (H4), no Channels continuation ownership (H7), no live metrics/health route proof (M6/M7), and no scheduled compaction (L3).

## Highest-leverage integration order

1. Finish the live-delivery seam together: C2 identity/`ts` confirmation, H7 per-page ownership, H4 shared scheduling, then emit M6 outcomes at those confirmed transitions.
2. Make durability non-optional (M7), which closes C3's missing-binding bypass and H1's request/alarm fail-open cases; wire real health at the same composition root.
3. Connect attachments and canonical context as one pipeline (H3, M3, M4), rather than separately landing unused helpers.
4. Move session links/cards into the shared final-delivery layer (M5, L1), schedule L3 compaction, and correct L2 documentation only after the production paths are true.

This census made no product-source or test changes and performed no deployment or live external mutation.
